---
import { getSocialUrl } from "@lib/urlShortener"
const { social } = Astro.params

const result = await getSocialUrl(`${social}`)

if (result.error === null) {
  return Astro.redirect("https://yaelo.dev")
} else {
  return Astro.redirect(`${result.result}`)
}
---
